input {
	kafka {
		codec => "json"
		topics => ["wiki-topic"]
	}
}

filter {
	json { 
		source => message 
		skip_on_invalid_json => true
		
	}
	
}

output {
	elasticsearch {
		hosts => [ "localhost:9200" ]
		user => "elastic"
		password => "changeme"
		index => "kafka-wiki4-%{+YYYY.MM.dd}"
		document_type => "wiki_kafka_test"
		codec => "json"
	}
	stdout { codec => rubydebug }
}	